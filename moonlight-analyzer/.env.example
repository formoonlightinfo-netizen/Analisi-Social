# Chiave API Claude — ottienila da https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-...

# Modello con capacità di visione da usare per l'analisi (opzionale, ha un default)
CLAUDE_MODEL=claude-sonnet-5

# Porta del server web locale (opzionale, default 3000)
PORT=3000
