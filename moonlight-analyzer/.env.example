# Porta del server web locale (opzionale, default 3000)
PORT=3000

# Non serve nessuna chiave API: l'analisi visiva dei video la fa direttamente
# Claude Code guardando i fotogrammi in chat (vedi CLAUDE.md), usando
# l'abbonamento Claude già attivo — nessun costo extra.
